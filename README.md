# `redux-amplitude-middleware`

Provides a middleware for mapping redux actions to amplitude events.

## Usage
```
import { createAmplitudeMiddleware } from 'redux-amplitude-middleware'

const store = createStore()
```
